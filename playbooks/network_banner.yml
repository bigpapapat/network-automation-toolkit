---
- name: set router banners
  hosts: network
  gather_facts: no

  tasks:
    - name: load banner onto network device
      vars:
        - network_banner:  "Testing Webhook 3"
        - banner_type: "{{ net_type | default('login') }}"
      include_role:
        name: "../roles/banner"
